import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#FFCCBB',
    height: '100%',
    marginBottom: 50,
  },
  wrapper: {
    alignItems: 'center',
  },
});
